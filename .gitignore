# Database (lives at ~/.massiemoney/data/ locally, not in repo)
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Checkpoint files
checkpoint.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# Output JSON (regenerated from DB, not checked in)
# Uncomment the line below if you want to keep output out of git.
# For now we check it in so the frontend can reference it.
# output/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.local
